# Meili public presentation site

- The existing root site is the Meili skill-card site. Keep it intact.
- The interactive Codex course lives under `course/` and uses relative assets.
- Preserve the five `template-sample-0[1-5].jpg` photos byte for byte. Do not substitute generated images.
- Chapter 12 is planning only and must not appear in the published course until approved and implemented.
- Before publishing, verify all 20 screen and menu entries, local asset paths, JavaScript syntax, and the public GitHub Pages URL.
